server:
  servlet:
    contextPath: /github-client-api
github:
  api:
    url: 'http://localhost:8080'
management:
  endpoints:
    web:
      exposure:
        include: '*'
feign:
  hystrix:
    enabled: true
  httpclient:
    connection-timeout: 3000